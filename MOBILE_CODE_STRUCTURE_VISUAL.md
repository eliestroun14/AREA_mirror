# Mobile Code Structure - Visual Guide

## Before Refactoring

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│          action-fields-page/[id].tsx (280 lines)        │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │ Imports (20 lines)                                 │ │
│  │ - React Native components                          │ │
│  │ - Types                                            │ │
│  │ - Utilities                                        │ │
│  │ - Components                                       │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │ State (30 lines)                                   │ │
│  │ - useState declarations                            │ │
│  │ - Multiple state variables                         │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │ Data Fetching (50 lines)                           │ │
│  │ - useEffect with fetch calls                       │ │
│  │ - Error handling                                   │ │
│  │ - State updates                                    │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │ Form Logic (20 lines)                              │ │
│  │ - handleFieldChange                                │ │
│  │ - Form data management                             │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │ Validation (30 lines)                              │ │
│  │ - Required field checking                          │ │
│  │ - Field conversion                                 │ │
│  │ - Error messages                                   │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │ JSX Rendering (100 lines)                          │ │
│  │ - Conditional renders                              │ │
│  │ - Inline styles                                    │ │
│  │ - Repeated patterns                                │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │ Styles (50 lines)                                  │ │
│  │ - StyleSheet.create                                │ │
│  │ - Many style objects                               │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
└──────────────────────────────────────────────────────────┘

❌ Problems:
- Everything in one file
- Hard to test
- Hard to reuse
- Hard to maintain
- Mixed concerns
```

## After Refactoring

```
┌─────────────────────────────────────────────────────────────────┐
│                  ORGANIZED STRUCTURE                            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ Page Layer (Composition - 100 lines)                            │
│                                                                 │
│  action-fields-page/[id].tsx                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ import { useActionData, useFormData } from '@/hooks'     │  │
│  │ import { validateRequiredFields } from '@/utils/...'     │  │
│  │ import { LoadingView, ErrorView } from '@/components/...'│  │
│  │                                                          │  │
│  │ const ActionFieldsPage = () => {                        │  │
│  │   // Get route params (5 lines)                         │  │
│  │   // Use custom hooks (3 lines)                         │  │
│  │   // Define handlers (10 lines)                         │  │
│  │                                                          │  │
│  │   // Render composition (60 lines)                      │  │
│  │   return (                                               │  │
│  │     <ActionFieldsPageHeader />                          │  │
│  │     <SourceSelectionCard />                             │  │
│  │     <ActionFieldsCard />                                │  │
│  │     <PrimaryButton />                                    │  │
│  │   );                                                     │  │
│  │ }                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            │               │               │
            ▼               ▼               ▼

┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────┐
│  HOOKS LAYER    │  │  UTILS LAYER    │  │  COMPONENTS LAYER   │
└─────────────────┘  └─────────────────┘  └─────────────────────┘

┌─────────────────┐  ┌─────────────────┐  ┌──────────────────────┐
│ useActionData   │  │formValidation.ts│  │ ATOMS (Basic UI)     │
│                 │  │                 │  │ ┌──────────────────┐ │
│ - Fetch service │  │ - validate      │  │ │ LoadingView      │ │
│ - Fetch action  │  │ - convert       │  │ │ ErrorView        │ │
│ - Handle errors │  │ - Pure funcs    │  │ │ PrimaryButton    │ │
│ - Return data   │  │                 │  │ └──────────────────┘ │
│                 │  │ ✅ Testable     │  │                      │
│ ✅ Reusable     │  │ ✅ No side fx   │  │ ✅ Reusable          │
└─────────────────┘  └─────────────────┘  └──────────────────────┘

┌─────────────────┐                      ┌──────────────────────┐
│ useFormData     │                      │ MOLECULES            │
│                 │                      │ (Composite UI)       │
│ - Form state    │                      │ ┌──────────────────┐ │
│ - Handle change │                      │ │ PageHeader       │ │
│ - Initialize    │                      │ │ SourceCard       │ │
│                 │                      │ │ FieldsCard       │ │
│ ✅ Reusable     │                      │ └──────────────────┘ │
└─────────────────┘                      │                      │
                                         │ ✅ Composed          │
                                         └──────────────────────┘

✅ Benefits:
- Separated concerns
- Easy to test each piece
- Reusable components
- Clear dependencies
- Maintainable code
```

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    USER INTERACTION                         │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│              ACTION FIELDS PAGE                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 1. Get route params (zapId, actionId, etc.)           │ │
│  └────────────────────────────────────────────────────────┘ │
│                        │                                    │
│                        ▼                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 2. useActionData({ serviceActionId, actionId })       │ │
│  │    → Fetches service and action from API              │ │
│  │    → Returns { service, action, loading, error }      │ │
│  └────────────────────────────────────────────────────────┘ │
│                        │                                    │
│                        ▼                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 3. useFormData()                                       │ │
│  │    → Manages form state                                │ │
│  │    → Returns { formData, handleFieldChange, init }    │ │
│  └────────────────────────────────────────────────────────┘ │
│                        │                                    │
│                        ▼                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 4. Render based on state                               │ │
│  │    if (loading) → <LoadingView />                      │ │
│  │    if (error) → <ErrorView />                          │ │
│  │    else → <Components with data />                     │ │
│  └────────────────────────────────────────────────────────┘ │
│                        │                                    │
│                        ▼                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 5. User fills fields                                   │ │
│  │    → handleFieldChange called                          │ │
│  │    → formData updated                                  │ │
│  └────────────────────────────────────────────────────────┘ │
│                        │                                    │
│                        ▼                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 6. User clicks Continue                                │ │
│  │    → validateRequiredFields(action, formData)          │ │
│  │    → if valid: navigate with data                      │ │
│  │    → if invalid: show alert                            │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## Component Hierarchy

```
ActionFieldsPage
│
├─ Stack.Screen (Header config)
│
└─ View (Container)
   │
   └─ ScrollView
      │
      ├─ ActionFieldsPageHeader
      │  ├─ Service Logo (Image)
      │  ├─ Action Name (Text)
      │  └─ Action Description (Text)
      │
      ├─ SourceSelectionCard
      │  ├─ Title (Text)
      │  ├─ Description (Text)
      │  └─ StepSourceSelector
      │     └─ Dropdown + Modal
      │
      ├─ ActionFieldsCard
      │  ├─ Title (Text)
      │  └─ Fields Container
      │     └─ ActionFieldCard (for each field)
      │        ├─ Field Label
      │        ├─ Input/TextArea/Select
      │        └─ VariableSelector
      │           └─ Button + Modal
      │
      └─ PrimaryButton
         └─ "Continue" button
```

## Import Organization

```typescript
// ✅ GOOD - New structure
import { useLocalSearchParams, Stack, router } from 'expo-router';
import { useEffect, useState } from 'react';

// Hooks (custom)
import { useActionData, useFormData } from '@/hooks';

// Utils (business logic)
import { validateRequiredFields, convertFieldsToArray } from '@/utils/formValidation';

// Components - Atoms (basic)
import { LoadingView, ErrorView, PrimaryButton } from '@/components/atoms';

// Components - Molecules (composite)
import {
  ActionFieldsPageHeader,
  SourceSelectionCard,
  ActionFieldsCard
} from '@/components/molecules';
```

```typescript
// ❌ BAD - Old structure
import { StyleSheet, Text, View, Image, TouchableOpacity, FlatList, ScrollView } from "react-native";
import { useLocalSearchParams } from "expo-router";
import { useEffect, useState } from 'react';
import { Service, Trigger, Action } from "@/types/type";
import { Stack } from 'expo-router';
import { imageMap } from "@/types/image";
import { getServiceImageSource } from "@/utils/serviceImageUtils";
import ActionFieldCard from "@/components/molecules/action-field-card/action-field-card";
import StepSourceSelector from "@/components/molecules/step-source-selector/step-source-selector";
import { TriggerField } from "@/types/type";
import { router } from "expo-router";
import { useApi } from "@/context/ApiContext";
// ... many more
```

## Testing Structure

```
tests/
├── hooks/
│   ├── useActionData.test.ts
│   │   ├── ✅ Test successful fetch
│   │   ├── ✅ Test error handling
│   │   └── ✅ Test loading states
│   │
│   └── useFormData.test.ts
│       ├── ✅ Test field changes
│       ├── ✅ Test initialization
│       └── ✅ Test form data structure
│
├── utils/
│   └── formValidation.test.ts
│       ├── ✅ Test validation logic
│       ├── ✅ Test field conversion
│       └── ✅ Test edge cases
│
└── components/
    ├── atoms/
    │   ├── LoadingView.test.tsx
    │   ├── ErrorView.test.tsx
    │   └── PrimaryButton.test.tsx
    │
    └── molecules/
        ├── ActionFieldsPageHeader.test.tsx
        ├── SourceSelectionCard.test.tsx
        └── ActionFieldsCard.test.tsx
```

## File Size Comparison

```
BEFORE:
┌────────────────────────────────────┐
│ action-fields-page/[id].tsx        │
│ ███████████████████████████████    │
│ 280 lines                          │
└────────────────────────────────────┘

AFTER:
┌────────────────────────────────────┐
│ action-fields-page/[id].tsx        │
│ ███████████                        │
│ 100 lines (-64%)                   │
└────────────────────────────────────┘

├─ useActionData.ts (60 lines)
├─ useFormData.ts (30 lines)
├─ formValidation.ts (40 lines)
├─ LoadingView.tsx (30 lines)
├─ ErrorView.tsx (35 lines)
├─ PrimaryButton.tsx (40 lines)
├─ ActionFieldsPageHeader.tsx (50 lines)
├─ SourceSelectionCard.tsx (40 lines)
└─ ActionFieldsCard.tsx (55 lines)

Total: 480 lines across 10 files
BUT: All pieces are reusable!
```

## Summary

### Before: Monolithic 🏚️
- 1 file, 280 lines
- Everything mixed together
- Hard to maintain
- Hard to test
- Hard to reuse

### After: Modular 🏗️
- 10 files, average 48 lines each
- Clear separation
- Easy to maintain
- Easy to test
- Easy to reuse

### Result: 🎉
- **Main file 64% smaller**
- **9 reusable components**
- **2 reusable hooks**
- **2 utility functions**
- **Clear architecture**
- **Production ready**
